---
phase: 01-clean-slate-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pages/home_hamburger/channel_screen/chats_screen.dart
  - lib/pages/home_hamburger/channel_screen/chat_item.dart
autonomous: true
requirements: [IDM-01, DB-01]
user_setup: []
must_haves:
  truths:
    - "No hardcoded _instructorChats list exists in chats_screen.dart"
    - "ChatType enum includes directMessage and excludes instructor"
  artifacts:
    - path: "lib/pages/home_hamburger/channel_screen/chats_screen.dart"
      provides: "Removed dummy instructor chat data"
      contains: "NO _instructorChats"
    - path: "lib/pages/home_hamburger/channel_screen/chat_item.dart"
      provides: "Updated ChatType enum"
      contains: "enum ChatType { groupChat, directMessage }"
  key_links:
    - from: "chat_item.dart"
      to: "DirectMessageItem"
      via: "ChatType.directMessage"
      pattern: "ChatType.directMessage"
---

<objective>
Remove hardcoded dummy instructor chats and update ChatType enum to support direct messages.

Purpose: Clean up frontend dummy data and establish enum for DM feature.
Output: Modified chats_screen.dart (no _instructorChats), modified chat_item.dart (updated ChatType).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\ROADMAP.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\REQUIREMENTS.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\phases\01-clean-slate-data-model\01-CONTEXT.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\phases\01-clean-slate-data-model\01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _instructorChats dummy data</name>
  <files>lib/pages/home_hamburger/channel_screen/chats_screen.dart</files>
  <read_first>lib/pages/home_hamburger/channel_screen/chats_screen.dart</read_first>
  <acceptance_criteria>
    - grep -c '_instructorChats' lib/pages/home_hamburger/channel_screen/chats_screen.dart returns 0
    - flutter analyze exits 0 (no compile errors)
  </acceptance_criteria>
  <action>Delete the _instructorChats const list (lines 114-130 per research). Remove all code rendering _instructorChats (e.g., ListView.builder for instructor chats). Ensure no references to _instructorChats remain in the file. Per IDM-01 and D-04.</action>
  <verify>grep -c '_instructorChats' lib/pages/home_hamburger/channel_screen/chats_screen.dart</verify>
  <done>_instructorChats list and all related rendering code removed from chats_screen.dart</done>
</task>

<task type="auto">
  <name>Task 2: Update ChatType enum</name>
  <files>lib/pages/home_hamburger/channel_screen/chat_item.dart</files>
  <read_first>lib/pages/home_hamburger/channel_screen/chat_item.dart</read_first>
  <acceptance_criteria>
    - grep -c 'enum ChatType { groupChat, directMessage }' lib/pages/home_hamburger/channel_screen/chat_item.dart returns 1
    - grep -c 'instructor' lib/pages/home_hamburger/channel_screen/chat_item.dart returns 0 (in ChatType context)
    - flutter analyze exits 0 (no compile errors)
  </acceptance_criteria>
  <action>Replace existing ChatType enum with `enum ChatType { groupChat, directMessage }`. Remove the `instructor` value per D-04. No other references to `ChatType.instructor` exist beyond _instructorChats (already deleted).</action>
  <verify>grep -c 'enum ChatType' lib/pages/home_hamburger/channel_screen/chat_item.dart</verify>
  <done>ChatType enum updated to include directMessage, instructor value removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → ChatsScreen | User interacts with chat list UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | ChatsScreen | Accept | No auth changes; Firestore rules handle access |
| T-01-02 | T | chats_screen.dart | Accept | No data modification; only remove dummy in-memory data |
</threat_model>

<verification>
1. `grep -c '_instructorChats' lib/pages/home_hamburger/channel_screen/chats_screen.dart` returns 0
2. `flutter analyze` passes with no errors
3. `grep -c 'enum ChatType { groupChat, directMessage }' lib/pages/home_hamburger/channel_screen/chat_item.dart` returns 1
</verification>

<success_criteria>
1. chats_screen.dart has no _instructorChats references (IDM-01 met)
2. ChatType enum includes directMessage, excludes instructor (DB-01 met)
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-slate-data-model/01-01-SUMMARY.md`
</output>
