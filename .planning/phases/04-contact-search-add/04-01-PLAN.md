---
phase: 04-contact-search-add
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified: [lib/pages/home_hamburger/channel_screen/chats_screen.dart]
autonomous: true
requirements: [IDM-06]
user_setup: []
must_haves:
  truths:
    - Search bar toggles in AppBar
    - Users collection cached in memory
    - Search filters by email/name (case-insensitive)
    - Search enforces existing @carsu.edu.ph regex
  artifacts:
    - path: lib/pages/home_hamburger/channel_screen/chats_screen.dart
      provides: Search bar UI, user cache, search logic
      contains: ["TextField", "usersCollection", "filterSearch"]
  key_links:
    - from: chats_screen.dart
      to: C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\lib\pages\login\login_screen.dart
      via: Email regex reuse
      pattern: "RegExp(r'^[a-zA-Z]+\\.[a-zA-Z]+@carsu\\.edu\\.ph$')"
    - from: chats_screen.dart
      to: FirebaseFirestore.instance.collection('users')
      via: Firestore query
      pattern: "FirebaseFirestore.instance.collection('users')"
---

<objective>
Add search bar UI and user search functionality to ChatsScreen.

Purpose: Enable users to search for contacts using email or name within @carsu.edu.ph domain.
Output: Modified chats_screen.dart with AppBar search toggle, cached users, local search filter.
</objective>

<execution_context>
@C:\Users\Sam\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Sam\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\PROJECT.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\ROADMAP.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\.planning\STATE.md
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\lib\pages\home_hamburger\channel_screen\chats_screen.dart
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\lib\pages\login\login_screen.dart
@C:\Users\Sam\Desktop\Knowledge Folder\Flutter Shenanigans\Final\luminescence\lib\pages\home_hamburger\channel_screen\direct_message_tile.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AppBar search toggle and TextField</name>
  <files>lib/pages/home_hamburger/channel_screen/chats_screen.dart</files>
  <action>Modify AppBar to include search icon (Icons.search) that toggles a TextField in the AppBar. Use StatefulWidget boolean _isSearching to track mode. When searching: show TextField with hint "Search contacts...", reuse AppColors for theming (not hardcoded colors), use withValues(alpha:) instead of deprecated withOpacity(). On close icon tap: clear search, set _isSearching = false.</action>
  <verify>
    <automated>flutter analyze lib/pages/home_hamburger/channel_screen/chats_screen.dart 2>&1 | grep -c "error"</automated>
  </verify>
  <done>AppBar has toggleable search TextField, uses AppColors, no analyzer errors</done>
</task>

<task type="auto">
  <name>Task 2: Fetch and cache users, implement local search</name>
  <files>lib/pages/home_hamburger/channel_screen/chats_screen.dart</files>
  <action>Add List<Map<String, dynamic>> _cachedUsers = []; to state. In initState: fetch FirebaseFirestore.instance.collection('users').get() and cache docs as maps with email, name, uid. Add TextEditingController _searchController, listen to changes to trigger _filterSearch(). Implement _filterSearch(): filter _cachedUsers where email matches regex ^[a-zA-Z]+\.[a-zA-Z]+@carsu\.edu\.ph$ (reuse pattern from login_screen.dart) and contains search query (case-insensitive) in email or name. Show filtered results in ListView below chat list when searching.</action>
  <verify>
    <automated>grep -c "usersCollection\|_cachedUsers\|filterSearch" lib/pages/home_hamburger/channel_screen/chats_screen.dart</automated>
  </verify>
  <done>Users cached on init, search filters by email/name with regex, results displayed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | User input (search query) crosses to user data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | users collection | mitigate | Query only needed fields (email, name, uid) via .select() |
| T-04-01-02 | Spoofing | Search input | accept | Client-side validation only, server-side Firestore rules enforce domain |
| T-04-01-03 | Denial of Service | Large users collection | mitigate | Cache once on init, limit search results to 50 max |
</threat_model>

<verification>
1. `flutter analyze` passes for chats_screen.dart
2. Search bar toggles, users cached, filter works as expected
</verification>

<success_criteria>
1. AppBar search icon toggles TextField
2. Users collection fetched and cached in memory
3. Search filters by email (enforces domain regex) and name (case-insensitive)
4. No deprecated withOpacity() or hardcoded colors used
</success_criteria>

<output>
After completion, create `.planning/phases/04-contact-search-add/04-01-SUMMARY.md`
</output>
